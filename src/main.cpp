#include <iostream>

int main( int argc, char** argv ) {

  // check if backend is running

  // if not, start it

  // parse command line arguments

  std::cout << "Hello nao" << std::endl;
  return 0;

}
